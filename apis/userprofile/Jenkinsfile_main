pipeline {
    agent {
        label 'main'
    }

    stages {
        stage('Build Docker Image') {
            steps {
                echo 'Building docker image'
                dir('apis/userprofile/') {
                	sh 'pwd'
                	sh 'sudo docker build -t willido/userprofile:1.0 .'
		}
            }
        }
    }
}
